<h1>All Artists</h1>
<div class="artists_list">
  <% @artists.each do |artist| %>
    <h3><%= link_to artist.name, artist_url(artist) %></h3>
  <% end %>
</div>
<%= link_to "New Artist", new_artist_path %>
